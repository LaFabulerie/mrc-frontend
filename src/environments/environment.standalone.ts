export const environment = {
  production: true,
  serverHost: 'http://localhost:8000',
  isStandalone: false, //if false, the app will run in web mode and apiKey must be set
  houseless: true, //can be set to true if this app is standalone
  isPrintable: true, //if true, the app will be able to print basket
  apiKey: null,
};
