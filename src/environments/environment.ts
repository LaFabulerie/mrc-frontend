export const environment = {
  production: true,
  serverHost: 'http://172.20.93.115:8080',
  isStandalone: true, //if false, the app will run in web mode and apiKey must be set
  houseless: true, //can be set to true if this app is standalone
  apiKey: null,
};
